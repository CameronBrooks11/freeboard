version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "05:00"
      timezone: "UTC"
    # Let a release settle before it is eligible for auto-merge, so a
    # compromised or immediately-yanked version is not published to ghcr on
    # the day it ships. Version updates only -- security updates ignore
    # cooldown, so advisory fixes still arrive the moment they exist.
    cooldown:
      semver-patch-days: 3
      semver-minor-days: 7
    labels:
      - "dependencies"
      - "npm"
    commit-message:
      prefix: "deps"
    open-pull-requests-limit: 8
    ignore:
      # monaco 0.56 dropped every CSS subpath export and rewrote `editor.api`
      # to statically pull the standalone editor and its CSS. packages/ui
      # imports the `esm/vs/...?worker` entries, none of which resolve under
      # the new exports map. Held at 0.55.x -- see openscad-web#254 for the
      # full analysis; revisit when monaco ships a CSS-free API entry.
      - dependency-name: "monaco-editor"
        update-types:
          - "version-update:semver-minor"
          - "version-update:semver-major"
    groups:
      npm-minor-patch:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "05:30"
      timezone: "UTC"
    cooldown:
      semver-patch-days: 3
      semver-minor-days: 7
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "deps"
    open-pull-requests-limit: 4
